version: "3.8"

services:
  flask:
    build: .
    image: flask
    container_name: flask-api
    volumes:
      - ../data/images:/app/data/images
    ports:
      - "5000:5000"
    env_file:
      - .env
